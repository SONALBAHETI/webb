export const UserReportCategory = {
  HARASSMENT: "harassment",
  SPAM: "spam",
  FRAUD: "fraud",
  INAPPROPRIATE_BEHAVIOUR: "inappropriate behaviour",
}

export const UserReportCategoryValues = Object.values(UserReportCategory);

export const UserReportStatus = {
  PENDING: "pending",
  REVIEWED: "reviewed",
  RESOLVED: "resolved",
}

export const UserReportStatusValues = Object.values(UserReportStatus);